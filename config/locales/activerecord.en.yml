en:
  activerecord:
    models:
      test: "Test"

    attributes:
      test:
        title: "Title"
        category: "Category"
        time_limit: "Time limit, s"

    errors:
      messages:
        blank: "can't be blank"
        required: "must exist"
